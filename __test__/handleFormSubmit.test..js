import { handleFormSubmit } from '../src/client/index';

describe('test "handleFormSubmit()" exists', () => {

    test('should return true', () => {

        expect(handleFormSubmit).toBeDefined();
    });
    
});

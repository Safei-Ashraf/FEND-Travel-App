import 'regenerator-runtime/runtime';
import { fillValue, get_GeoNamesInfo } from '../src/client/js/app.js';

describe('test "fillValue()" exists', () => {

    test('should return true', () => {

        expect(fillValue).toBeDefined();
    });
    
});
describe('test "get_GeoNamesInfo()" exists', () => {

    test('should return true', () => {

        expect(get_GeoNamesInfo).toBeDefined();
    });
    
});
